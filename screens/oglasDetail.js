import React from 'react';
import { StyleSheet, View, Text, Image } from 'react-native';

export default function OglasDetail({ navigation }) {
  //const rating = navigation.getParam('rating'); get param snippet
  
  return (
    <View>
      <Text>Oglas detaljno</Text>
    </View>
  );
}

const styles = StyleSheet.create({
});